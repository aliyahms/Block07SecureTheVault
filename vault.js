// The introduction of the vault
let intro =
  "You have received this message because you have been chosen to open an important vault. Here is the secret combination:";
// The combinatin is 10-40-39
// Below are the calculations for the user to learn each code
  const combination1 = 5 + 5;
const combination2 = 10 * 4;
const combination3 = 40 - 1;

const combinationLock = combination1 + "-" + combination2 "-" + combination3;

// This will show the user the message without opening the console and the combination hint calculations
alert(intro);
alert(combinationLock);
